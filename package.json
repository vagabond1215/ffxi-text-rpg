{
  "name": "hearth-and-horizon",
  "version": "0.9.500",
  "private": true,
  "type": "module",
  "description": "Original text-first persistent fantasy life RPG with deterministic simulation, exploration, livelihoods, relationships, and dangerous travel.",
  "scripts": {
    "serve": "node scripts/serve.js",
    "start": "npm run serve",
    "test": "node --test",
    "benchmark": "node scripts/benchmark.js",
    "benchmark:sample": "node scripts/benchmarkSample.js",
    "bench": "npm run benchmark",
    "census": "node scripts/contentCensus.js",
    "audit:repo": "node scripts/repositoryAudit.js",
    "hardening": "node --test tests/longSessionLifecycle.test.js && npm run benchmark:sample",
    "check": "npm run audit:repo && npm test && npm run census && npm run benchmark && npm run benchmark:sample"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=24"
  }
}
